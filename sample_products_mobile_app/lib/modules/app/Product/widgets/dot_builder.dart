import 'package:flutter/material.dart';

class DotBuilder extends StatefulWidget {
  @override
  _DartBuilderState createState() => _DartBuilderState();
}

class _DartBuilderState extends State<DotBuilder> {
  final pageViewController = PageController(initialPage: 1);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
